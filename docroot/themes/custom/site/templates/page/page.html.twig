<!-- Begin - wrapper -->
<div class="layout__wrapper layout--fixed-header" id="page-html">
    {{ drupal_block('popup_cta') }}
    <!-- Begin - drawer -->
    <div class="layout__drawer" aria-hidden="true">

        <!-- Begin - title -->
        <span class="layout-title">Title</span>
        <!-- End - title -->

    </div>
    <!-- End - drawer -->

    {# Begin - header #}
    {% include '@site/_partials/header.html.twig' %}
    {# End - header #}

    <!-- Begin - document -->
    <main class="layout__document" role="document">

        <!-- Begin - content -->
        <div class="layout__content">

            {# Begin - banner #}
            {% if is_front %}

                {% if page.banner %}
                    {% block banner %}
                        <div class="banner">
                            {{ page.banner }}
                        </div>
                    {% endblock %}
                {% endif %}

            {% else %}

                {% if node.field_page_main_image.value or node.field_vih_course_image.value %}
                    <div class="banner banner--small">
                        <div class="banner__inner">

                            {# Begin - image #}
                            {% if node.field_page_main_image.value %}
                                <img src="{{ node.field_page_main_image.entity.fileuri | image_style('banner_small') }}" alt="">
                            {% elseif node.field_vih_course_image.value %}
                                <img src="{{ node.field_vih_course_image.entity.fileuri | image_style('banner_small') }}" alt="">
                            {% endif %}
                            {# End - image #}

                            <div class="banner__body">
                                <div class="container">
                                    {% if node.title.value %}
                                        <h1 class="banner__heading__title">{{ node.title.value }}</h1>
                                    {% elseif page['#title'] %}
                                        <h1 class="banner__heading__title">{{ page['#title'] }}</h1>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </div>
                {% endif %}
            {% endif %}
            {# End - banner #}

            {# Begin - header #}
            {% if page.header is defined and page.header | render | striptags | trim %}
                {% block header %}
                    <section class="sectioned sectioned--header sectioned--small-inner-spacing">
                        <div class="sectioned__inner">
                            <div class="container">
                                {{ page.header }}
                            </div>
                        </div>
                    </section>
                {% endblock %}
            {% endif %}
            {# End - header #}

            <!-- Begin - content -->
            <div{{ content_attributes.addClass(content_classes) }}>
                {% block content %}
                    <!-- Begin - content -->
                    <a id="main-content"></a>

                    {{ page.content }}
                    <!-- End - content -->
                {% endblock %}
            </div>
            <!-- End - content -->

        </div>
        <!-- End - content -->

        {# Begin - footer #}
        {% include '@site/_partials/footer.html.twig' %}
        {# End - footer #}

    </main>
    <!-- End - document -->

    <!-- Begin - obfuscator -->
    <div class="layout__obfuscator"></div>
    <!-- End - obfuscator -->

</div>
<!-- End - wrapper -->
